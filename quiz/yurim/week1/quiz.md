[4장]
Q1. var 키워드로 변수를 선언하면 런타임 이전에 자바스크립트 엔진에 의해 변수 선언과 초기화가 동시에 이루어집니다. 이때 암묵적으로 할당되는 값은 무엇인가요?

[12장]
Q2. 함수 선언문(Function Declaration)은 함수 전체가 호이스팅되지만, 변수에 함수를 할당하는 함수 표현식(Function Expression)은 변수 호이스팅만 발생합니다. 그렇다면 함수 표현식으로 정의된 함수를 선언문 이전에 호출하면 어떤 에러가 발생할까요?

Q3. 자바스크립트에서 함수는 객체이지만 일반 객체와는 다릅니다. 일반 객체는 호출할 수 없지만 함수는 호출할 수 있는데, 이는 함수가 어떤 내부 메서드를 가지고 있기 때문인가요?

[13장]
Q4. 자바스크립트는 함수를 '어디서 호출했는지'가 아니라 '어디서 정의했는지'에 따라 상위 스코프를 결정합니다. 이 방식을 무엇이라고 하나요?

Q5. 스코프가 계층적으로 연결된 것을 스코프 체인이라고 합니다. 자식 스코프에서 선언되지 않은 변수를 참조할 때 자바스크립트 엔진은 어떤 방향으로 변수를 검색해 나가나요?

[14장]
Q6. 전역 변수는 생명 주기가 애플리케이션과 일치하여 메모리를 오래 점유합니다. 또한 스코프 체인의 종점에 존재하기 때문에 변수 검색 속도가 가장 (빠르다/느리다) 중 맞는 것은?

Q7. 전역 변수의 사용을 억제하기 위해 파일 자체를 독자적인 모듈 스코프로 만드는 ES6의 기능은 무엇인가요?

[15장]
Q8. 다음 코드의 출력 결과를 예측해 보세요. 
    (+정답의 이유는?)

let x = 1;
if (true) {
  console.log(x); // 여기서 어떤 결과가 나올까요?
  let x = 2;
}